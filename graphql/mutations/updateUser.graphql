#import "../fragments/userInfo.graphql"

#import "../fragments/userInfo.graphql"

mutation updateUser(
  $email: String
  $firstName: String
  $lastName: String
  $password: String
  $currentPassword: String
) {
  updateUser(
    email: $email
    firstName: $firstName
    lastName: $lastName
    password: $password
    currentPassword: $currentPassword
  ) {
    ...UserInfo
  }
}
